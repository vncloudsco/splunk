/*
there are cases where we need to identify the viz type from a reportModel, and do something based on
the type of viz. For example, different viz types have different drilldown tokens.
 */
export const CHART = 'chart';
export const SINGLE_VALUE = 'single';
export const MAP = 'map';
export const CUSTOM_VIZ = 'viz';
export const EVENT = 'event';
export const TABLE = 'table';